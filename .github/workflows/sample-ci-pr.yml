name: Sample CICD - PR
on: [push, workflow_dispatch]
jobs:
  Build:
    runs-on: self-hosted
    steps:
      - run: echo OK
  Test:
    runs-on: self-hosted
    needs: Build
    steps:
      - run: echo OK
